# Immutable legacy knowledge packs

The `v0.1.0` and `v0.2.0` development packs remain at their original paths because their manifests and fingerprints are immutable. Their lifecycle is recorded in `knowledge/registry/pack_registry.yml`.

Legacy packs are not runtime eligible and are not copied into this directory. They may be used only as traceable migration inputs for a new canonical version.
